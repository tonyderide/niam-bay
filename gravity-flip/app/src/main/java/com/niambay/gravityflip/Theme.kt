package com.niambay.gravityflip

import androidx.compose.ui.graphics.Color

object GravityColors {
    val Background = Color(0xFF0A0A1A)
    val Player = Color(0xFF00F0FF)       // Cyan neon
    val PlayerGlow = Color(0x4000F0FF)   // Cyan glow (translucent)
    val Obstacle = Color(0xFFFF00AA)     // Magenta neon
    val ObstacleGlow = Color(0x40FF00AA)
    val ScoreText = Color.White
    val ParticleCyan = Color(0xFF00F0FF)
    val ParticleMagenta = Color(0xFFFF00AA)
    val FlashWhite = Color(0x33FFFFFF)
    val MenuSubtext = Color(0xFFAAAAAA)
    val DeathFlash = Color(0xFFFF2222)
}
