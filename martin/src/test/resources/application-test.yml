spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

kraken:
  futures:
    rest-url: https://localhost
    ws-url: wss://localhost
    demo-rest-url: https://localhost
    demo-ws-url: wss://localhost
    use-demo: true
    api-key: test-key
    api-secret: test-secret
